import { TodoDoneDirective } from './todo-done.directive';

describe('TodoDoneDirective', () => {
  it('should create an instance', () => {
    const directive = new TodoDoneDirective();
    expect(directive).toBeTruthy();
  });
});
